{
	"label": "<%= (@user.name.nil? ? 'none' : @user.name) + ' ' + @user.email %>",
	"name": "<%= (@user.name.nil? ? 'none' : @user.name)  %>",
	"email": "<%= @user.email  %>",
	"avatar": "<%= @user.avatar  %>"
}
