[
	<% @users.each do |user| %>
		{
			"label": "<%= user.name + ' ' + user.email %>",
			"id": "<%= user.id  %>",
			"name": "<%= user.name  %>",
			"email": "<%= user.email  %>",
			"avatar": "<%= user.avatar  %>"
		}
	<% end %>
]

