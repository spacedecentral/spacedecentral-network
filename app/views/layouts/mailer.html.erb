<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <% if @mailer_environment == "production" %>
       <img src="https://spacedecentral.net/<%= image_path("logo.png") %>" style="width:170px;height:auto;" />
    <% else %>
       <img src="https://staging.spacedecentral.net/<%= image_path("logo.png") %>"  style="width:170px;height:auto;" />
    <% end %>
   
    <%= yield %>
  </body>
</html>
