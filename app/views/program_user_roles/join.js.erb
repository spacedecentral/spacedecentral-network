<% if !@program_user_role.errors.empty? %>
  $("#join_program_errors").html("<%= j (render 'program_user_role_errors') %>").show();
<% else %>
  //$("#join_program_modal").modal('hide');
  $('.flash').html("<%= j render('shared/flashages', notice: 'You have succesfully joinde the program.')%>");
  window.location.reload(true); 
<% end %>
